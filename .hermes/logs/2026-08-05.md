# 2026-08-05

**项目**: astro-blog
**分支**: main

## 改动摘要
- 光标系统：移除编辑状态（白色哑铃形），恢复默认菱形徽章 + loading 双环两态；测试按钮改回两态循环
  - 修复选择器 bug：EDITABLE_SELECTOR 数组 join(', ') 把 :not() 排除链拆成独立选择器（匹配所有元素导致默认一直是编辑态），改为单字符串拼接
- 导航栏 hover 修复：原内联 style 优先级高于 hover 类导致 hover 从不生效，去掉内联改用 Tailwind 类（text-[#50787d]/60 + hover:text-[#49B1F5]）；site-header.js 未滚动分支移除内联样式交回 CSS
- 笔记页新增 3 个条目：
  - SVG 变化（svg.createDrawable 逐笔绘制粗线条字）
  - 文本自定义模板（splitText 三面模板 + createTimeline 3D 翻转）
  - 导航栏 3D 翻转（模拟导航栏效果，平滑往返版）
- 导航栏 Logo 动画：3D 翻转经过多轮调参（perspective 透视抖动、transform-origin Z 位移、字号、loop 跳变），最终定为平滑往返版：rotateX 0 → -90 → 0 + 三面透明度同步往返，终点=起点 loop 无跳变；无 perspective；Z=1.1rem；字号回 text-lg
- 新增实现详解文章：导航栏文字动画实现详解（含 manda 逐字上滚版，三大坑总结）

## 涉及文件
- `src/layouts/BaseLayout.astro` — 光标两态、导航 hover、logo 平滑往返动画、3D 样式
- `src/pages/notes.astro` — 光标测试按钮两态
- `public/scripts/site-header.js` — hover 修复、未滚动分支移除内联
- `src/data/anime-topics.ts` — 新增 3 个条目（含缩进/转义修复）
- `src/content/posts/nav-logo-animation-implementation.md` — 新文章

## 提交
- d398313 → 7552535（2 次，已推送）

## 踩坑记录
- anime-topics.ts 是 CRLF 行尾，patch 多行匹配时缩进会丢失，需 node 脚本兜底修复
- desc 模板字符串内反引号转义写 \\`（双反斜杠）会导致 build 报 `Expected "}" but found "svg"`，应为 \`
- 光标 EDITABLE_SELECTOR：数组 join 会在 :not() 链中插入逗号，变成匹配所有元素的选择器
- 3D 翻转抖动主因是 loop 重置跳变（-90° 跳回 0°），不是 transform-origin Z 值；平滑往返（终点=起点）解决

---

**项目**: manda-website (website-v2)
**分支**: master

## 改动摘要
- 导航栏 Logo 文字动画：先 3D 翻转（splitText 三面模板），调参后（转速 750→1500、translateY 下移、perspective 透视导致抖动已去掉、transform-origin Z 多轮调整）最终改为逐字上滚 clip 动画（chars: { wrap: 'clip' } + y 100%→0%→-100%）
- 文案由"曼大优学"改为"曼大优学 - 专注教育"
- 组件从 LogoText3D 改名 LogoTextRoll，React.memo 隔离 splitText DOM（防滚动 re-render 破坏）

## 涉及文件
- `src/components/LogoTextRoll.tsx` — 新组件（替换 LogoText3D）
- `src/components/Header.tsx` — import/文案更新
- `src/app/globals.css` — 3D 样式清理、clip 上滚样式

## 提交
- 50b4abb（已推送）

## 踩坑记录
- React 中 splitText 会破坏 React 维护的 DOM，需 memo 子组件隔离（props 只有静态文本）
- perspective 透视投影在小空间（导航栏 56px）放大翻转变形感，应去掉或 2000px+
