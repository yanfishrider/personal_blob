# 2026-08-22

**项目**: astro-blog
**分支**: main（本次提交）

## 改动摘要

### 1. 第七段老虎机竖条 + 5×5 网格排队绕行（补录 2026-08-21 完成内容）
- `src/pages/animation-lab.astro`：竖条 6 块步进滚动（moveMs 900 / pauseMs 1400）→ 底部方块变扁弹射（副本，随机圆弧色）→ 5×5 网格外圈绕行 → 内圈 → 中心 (2,2) 缩小淡出
- 相位驱动 + fresh 方块防重叠；网格默认填满暗紫

### 2. 第八段风水转盘（新增，复位解谜玩法）
- 8 同心环 × 每环 8 段弧，颜色 = 外层 8 段弧色固定顺序（对齐基准），初始随机旋转 offset 格打乱
- 点击环 → 该环弧顺时针转 1 格（45°，smoothstep 420ms）+ 联动环转 k 格（k ∈ {1,2} 随机）
- 联动：必定 1 个、最多 2 个独立环（无出边）；其余环各 1 条单向出边，**入度 ≤ 1**（每环最多被 1 个环关联）；目标随机 ≠ 自己
- 数字提示：hover 环正上方显示 +1，联动目标环显示 +k，移开消失
- 对齐反馈：环复位（cur % 360 ≡ 0）时弧 brightness 1.25 → 2 提亮
- 尺寸（多轮迭代后）：rMin 25 / rMax 249（等差步进 32）、弧宽 18、间距 14、最外环直径 498（660 内圈内）
- **坑记录**：
  - 命中层用实心透明圆 → 最外环遮挡全部内环（点击只转最外环）→ 改环形 stroke 带（fill none + pointer-events stroke）
  - 颜色打乱改法：不再打乱序列，改为固定外层顺序 + 初始旋转错位（保证"转回即对齐"）
  - 缩小/加宽迭代中命中带宽必须 < 半径步进，否则相邻环点击区域重叠

### 3. 笔记页 Three.js 学习区移除
- `src/data/three-topics.ts` 删除（653 行）
- `src/data/anime-topics.ts` 移除 threeTopics 导入与合并
- `src/pages/notes.astro` 标题 description 改「anime.js 交互式学习」

## 涉及文件
- `src/pages/animation-lab.astro`（第七段 slots + 第八段 fengshui）
- `src/data/anime-topics.ts` / `src/data/three-topics.ts`（删除）/ `src/pages/notes.astro`

## 验证
- npm run build 27 页通过
- Node 模拟：入度 ≤ 1 零违规、复位可达、几何不重叠
- 未提交：scripts_fix_slots*.py 等调试脚本（一次性产物，保留在工作区）
